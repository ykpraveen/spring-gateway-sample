name: Main Branch Build

on:
  push:
    branches: [main]

concurrency:
  group: main-build
  cancel-in-progress: false

jobs:
  backend:
    name: Build & test Maven modules
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - name: Build and test all modules
        run: mvn -B verify

  frontend:
    name: Build web-ui
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: web-ui/package-lock.json

      - name: Install dependencies
        working-directory: web-ui
        run: npm ci

      - name: Build
        working-directory: web-ui
        run: npm run build

  docker-images:
    name: Build ${{ matrix.service }} image
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: gateway
            context: .
            dockerfile: gateway/Dockerfile
          - service: api-server
            context: .
            dockerfile: api-server/Dockerfile
          - service: product-service
            context: .
            dockerfile: product-service/Dockerfile
          - service: pricing-service
            context: .
            dockerfile: pricing-service/Dockerfile
          - service: web-ui
            context: web-ui
            dockerfile: web-ui/Dockerfile
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      # Build-only: confirms every Dockerfile still builds on main. Nothing is pushed to a
      # registry — publishing images is a separate decision this repo hasn't made yet.
      - name: Build image (no push)
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: false
          tags: gateway-sample/${{ matrix.service }}:${{ github.sha }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}
